import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <h1>Weather Client</h1>
    <p>This is a minimal Angular app served from the ASP.NET Core project.</p>
  `
})
export class AppComponent { }
